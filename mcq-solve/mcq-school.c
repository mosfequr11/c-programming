#include <stdio.h>
#include <math.h>

int main()
{

  // 1
  // for (i = 3; i <= 15; i = i + 2)
  // {
  //   printf("ICT: %d \n", i);
  // }

  // 8
  // int i;
  // int s = 0;
  // for (i = 1; i <= 6; i = i + 1)
  // {
  //   s = s + i;
  // }
  // printf("%d \n", s);

  int x = 100;
  x = x / 20;
  printf("%d", (int)pow(x, 2));

  // return 0;
}
